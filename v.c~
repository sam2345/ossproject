#include<stdio.h>
#include<stdlib.h>

int main()
{
	int *p,tmp,t,i;
	scanf("%d",&t);
	tmp=t;
	p=(int *)malloc(sizeof(int *)*t);
	while(t--)
	{	scanf("%d",&p[i]);
		i++;
	}
	for(i=0;i!=tmp;i++)
	printf("%d",p[i]);
	free(p);
	return;

}
